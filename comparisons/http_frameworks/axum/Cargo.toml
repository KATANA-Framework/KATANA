[package]
name = "katana_comparison_axum"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.8", features = ["http1", "json", "tokio"] }
tokio = { version = "1", features = ["full"] }
